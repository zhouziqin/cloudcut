spring:
  application:
    name: elsearch

  cloud:
    config:
      enabled: true
      profiles: default
      label: ${spring.application.name}
      name: jdbc,message,pathpatterns,redis,webserver,order.timeout,elasticsearch
      #uri: http://config.idinbao.com:9999
      uri: http://localhost:9999