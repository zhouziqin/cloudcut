spring:
  application:
    name: redis

  cloud:
    config:
      enabled: true
      profiles: default
      label: ${spring.application.name}
      name: redis,dubbo
      #uri: http://config.idinbao.com:9999
      uri: http://localhost:8888